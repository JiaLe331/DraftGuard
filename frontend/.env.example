# Base URL of the deployed DraftGuard API (no trailing slash).
# Leave empty for local development, where Vite proxies /api to 127.0.0.1:8000.
VITE_API_BASE_URL=https://draftguard-api-XXXXXXXX.a.run.app
