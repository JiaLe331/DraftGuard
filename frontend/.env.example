# API origin only, without /api. Leave blank for the local Vite proxy.
# The dataset mailbox requires the development API; no browser fixture fallback.
# Example for a separately hosted API: https://api.example.com
VITE_API_BASE_URL=
